# Web App

This repository currently keeps the web app at the repo root. If you migrate to a Vite-based web app inside `apps/web`, this `vite.config.ts` already proxies `/api` to `http://localhost:8787`.
